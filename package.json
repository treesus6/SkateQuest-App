{
  "name": "skatequest-app",
  "version": "1.0.0",
  "private": true,
  "description": "SkateQuest static site",
  "scripts": {
    "serve-local": "python3 -m http.server 8000",
    "start": "npm run serve-local",
    "test": "echo \"No tests configured for this static site\" && exit 0"
  },
  "dependencies": {},
  "devDependencies": {}
}